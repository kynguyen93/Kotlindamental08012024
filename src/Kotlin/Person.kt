package Kotlin

class Person(
    var name: String,
    var age: Int,
    var address: String? = null
    ) {
    // Thuộc tính
    //var name: String = ""
    //var age: Int = 0

    // Phương thức khởi tạo (constructor)
    // Để ràng buộc các tham số cho việc khởi tạo

    // Hành vi
}